#include "lib/macro.h"
#include "nn/module.h"
#include "nn/network.h"
#include "nn/nn.h"

#define DEBUG

int main(void) {

  printf("%zu\n", sizeof(sModule));
  printf("%zu\n", sizeof(sNetwork));

  return 0;
}